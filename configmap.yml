apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config
  namespace: mysql
data:
  my.cnf: |
    [mysqld]
    max_connections = 200
    sql_mode = STRICT_ALL_TABLES
    default_authentication_plugin = mysql_native_password
